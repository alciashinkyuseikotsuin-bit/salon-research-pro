# 売上100万講座 AIスタッフルーム セットアップ手順

このフォルダには、AIスタッフルームの全ソースコードが入っています。
以下の手順で、あなた専用の同じツールを公開できます。所要時間30〜40分。

---

## 全体像

- **本体（このコード）** … Vercel（無料枠あり）にデプロイして公開します
- **AI部分** … Claude APIキー（従量課金）を1つ用意します
- **台本・ロープレ・LINE配信** … ChatGPTでGPTを3つ作ります（`gpts/` の設定文を貼るだけ）

---

## STEP 1. Claude APIキーを用意する

1. https://console.anthropic.com/ に登録
2. 「API Keys」→「Create Key」でキーを発行（`sk-ant-...`）
3. 支払い方法を登録し、少額（$5〜）チャージ
   - 目安: 受講生1人あたり月50〜100円程度の利用

## STEP 2. GitHubにこのコードを置く

1. https://github.com/ でリポジトリを新規作成（Private可）
2. このフォルダをそのままpush
   ```bash
   cd salon-research-pro
   git init && git add -A && git commit -m "init"
   git branch -M main
   git remote add origin <あなたのGitHubリポジトリURL>
   git push -u origin main
   ```
   ※ `git` が入っていない場合は、GitHubのWeb画面からフォルダごとドラッグ&ドロップでもOK

## STEP 3. Vercelにデプロイする

1. https://vercel.com/ にGitHubアカウントで登録
2. 「Add New → Project」→ STEP2のリポジトリを選ぶ
3. **環境変数を追加**（重要）
   - Name: `ANTHROPIC_API_KEY`
   - Value: STEP1で発行したキー（`sk-ant-...`）
4. 「Deploy」を押す → 数分で `https://〜.vercel.app` が発行されます

これで本体は公開完了です。

## STEP 4. GPTを3つ作る（台本・ロープレ・LINE配信）

ChatGPT（有料のPlus推奨）で、`gpts/` フォルダの設定文を使ってGPTを3つ作ります。

各ファイルの `---` から下の本文を全文コピーし、ChatGPTの
「GPTを作成する → 構成 → 指示（Instructions）」欄に貼り付けるだけです。

| 作るGPT | 使う設定文ファイル |
|---|---|
| カウンセリング台本 | `gpts/01_カウンセリング台本作成GPT_設定文.md` |
| LINE配信（ステップ＋通常） | `gpts/02_LINE配信GPT_設定文.md` |
| 接客ロープレ | `gpts/03_接客ロープレGPT_設定文.md` |

作成後、それぞれの共有URLをコピーします。

## STEP 5. GPTのURLをツールに設定する

`static/index.html` を開き、`GPTS_URLS` を探して3つのURLを入れます。

```javascript
const GPTS_URLS={
    script:'ここにカウンセリング台本GPTのURL',
    roleplay:'ここに接客ロープレGPTのURL',
    line:'ここにLINE配信GPTのURL',
};
```

変更をGitHubにpushすれば、Vercelが自動で再デプロイします。

---

## 完成

あなたのURL（`https://〜.vercel.app`）を受講生に配れば完成です。

## 困ったときは

- 生成が動かない → Vercelの環境変数 `ANTHROPIC_API_KEY` が正しいか確認
- GPTボタンが反応しない → STEP5のURL設定を確認
- コードの意味を知りたい → `IMPROVEMENT_LOOP.md` に開発の全経緯が残っています

## 各ファイルの役割（参考）

| ファイル | 役割 |
|---|---|
| `app.py` | サーバー本体（全APIの入り口） |
| `static/index.html` | 画面すべて（UIとJavaScript） |
| `config.py` | AIの頭脳（講座メソッド・薬機法ガード・刺さる言葉・広告基準） |
| `ai_*.py` | ペルソナ/商品/コピー/検索パターン生成 |
| `checkup.py` | 数字診断（ルール判定＋AI講評） |
| `ad_checker.py` | 広告診断（ルール判定＋AI分析） |
| `*_scraper.py` / `analyzer.py` | 悩みリサーチ（知恵袋・小町・検索ボリューム） |
| `gpts/` | 台本・ロープレ・LINEのGPT設定文 |
